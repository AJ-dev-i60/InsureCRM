<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .content {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 5px;
            border: 1px solid #dee2e6;
        }
        .highlight {
            color: #dc3545;
            font-weight: bold;
        }
        .priority-high {
            color: #dc3545;
        }
        .priority-medium {
            color: #ffc107;
        }
        .priority-low {
            color: #28a745;
        }
        .footer {
            margin-top: 20px;
            font-size: 0.9em;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>Task Reminder</h2>
    </div>
    
    <div class="content">
        <p>Dear <%= broker.firstName %>,</p>
        
        <p>This is a reminder about your upcoming task:</p>
        
        <div class="task-details">
            <h3><%= task.title %></h3>
            <p><%= task.description %></p>
            
            <ul>
                <li>Due Date: <span class="highlight"><%= dueDate %></span></li>
                <li>Priority: <span class="priority-<%= task.priority.toLowerCase() %>"><%= task.priority %></span></li>
                <% if (task.clientName) { %>
                <li>Client: <%= task.clientName %></li>
                <% } %>
                <% if (task.policyNumber) { %>
                <li>Related Policy: <%= task.policyNumber %></li>
                <% } %>
            </ul>
        </div>
        
        <p>Please ensure this task is completed by the due date. You can view more details and update the task status in your InsureCRM dashboard.</p>
    </div>
    
    <div class="footer">
        <p>This is an automated message from InsureCRM. Please do not reply to this email.</p>
    </div>
</body>
</html>
